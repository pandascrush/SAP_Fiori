specVersion: "4.0"
metadata:
  name: fiori_peace

type: application

framework:
  name: SAPUI5
  version: "1.126.2"
  libraries:
    - name: sap.m
    - name: sap.ui.core
    - name: sap.f

resources:
  configuration:
    paths:
      webapp: webapp

builder:
  resources:
    excludes:
      - "/test/**"  # Exclude test resources from the build
